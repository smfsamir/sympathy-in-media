// when the link is clicked, we need to check if annotations are available already, and load them if they are.
import Link from 'next/link'


const ArticleLink: React.FC = (articleName) => {
    return <Link href='/'>articleName</Link>
};